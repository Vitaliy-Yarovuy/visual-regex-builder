import { BlockTypesPipe } from './block-types.pipe';

describe('BlockTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new BlockTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
