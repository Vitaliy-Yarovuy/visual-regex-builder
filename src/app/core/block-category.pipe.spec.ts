import { BlockCategoryPipe } from './block-category.pipe';

describe('BlockCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new BlockCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
